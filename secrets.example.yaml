# Copy to secrets.yaml (kept out of git) and fill in.
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Native API encryption key. Generate a fresh one - never reuse someone else's:
#   https://esphome.io/components/api.html#configuration-variables
# (32 random bytes, base64). Home Assistant asks for this when adopting the device.
api_encryption_key: "PUT_YOUR_OWN_BASE64_KEY_HERE"
